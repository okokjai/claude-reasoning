# Execution Trail Enforcement Implementation Plan

> **For Claude:** Use `${SUPERPOWERS_SKILLS_ROOT}/skills/collaboration/executing-plans/SKILL.md` to implement this plan task-by-task.

**Goal:** 讓 claude-reasoning 的推理流程從「模型自律」升級為「環境強制」——透過 Claude Code hooks 生成執行契約、紀錄工具調用軌跡、在模型停止前強制閘門檢查，使「跳步驟」和「偽造執行記錄」在過程層 100% 被攔截。

**Architecture:** 三層 harness：
1. **Contract（契約層）**— `UserPromptSubmit` hook 偵測 `/claude-reasoning` prompt，生成該 session 的執行契約（必需工具序列）
2. **Trail（軌跡層）**— `PostToolUse` hook 在每次工具調用後由環境追加 JSONL 記錄（模型不參與生成）
3. **Gate（閘門層）**— `Stop` hook 在模型每次停止前對比 trail vs contract，缺失即 block 並注入缺失清單，模型必須補做才能停止

**Tech Stack:** Claude Code hooks（settings.json）、Bash（Git Bash）、Python 3.14（JSON 解析）、無新外部依賴

**核心不變式：**
- 模型不寫 trail、不寫 contract、不寫 gate 判定
- Stop gate 有 kill switch：contract 不存在 → 直接放行（不干擾普通 session）
- 硬性契約只有 A2 的 `mcp__unified-fetch__status`（A2 執行規則 1「Must actually call」，無例外）
- search/scrape 檢查為 `--runtime` 的 WARN 級別，避免誤殺純計算推理

---

## 任務 1: 建立安全網 + 確認 git 狀態

**Files:**
- Verify: `claude-reasoning/` git repo

**Step 1: 確認 git 狀態**

Run:
```bash
cd ~/.claude/skills/claude-reasoning && git status && git log --oneline -1
```
Expected: clean working tree, master 分支

**Step 2: 建立 runtime 工具目錄（git 追蹤）**

Run:
```bash
mkdir -p ~/.claude/skills/claude-reasoning/scripts
```
Expected: 目錄存在（scripts/ 已存在）

**Step 3: Commit**

```bash
git add -A && git commit -m "chore: baseline before execution trail enforcement" || true
```

---

## 任務 2: 寫 contract-gen.py（UserPromptSubmit hook 生成器）

**Files:**
- Create: `scripts/contract-gen.py`

**Step 1: 寫完整腳本**

```python
#!/usr/bin/env python3
"""Contract Generator — UserPromptSubmit hook.

Reads stdin JSON {prompt, session_id, ...}. If the prompt invokes
claude-reasoning, writes an execution contract for this session.

Model never writes this file; the environment generates it.
"""
import json
import os
import re
import sys
import time

CONTRACTS_DIR = os.path.expanduser("~/.claude/reasoning-contracts")

# Hard contract: A2 execution rule 1 "Must actually call mcp__unified-fetch__status"
HARD_REQUIRED = [
    {
        "stage": "A2",
        "tool": "mcp__unified-fetch__status",
        "min_count": 1,
        "why": "A2 contract execution rule 1: Must actually call unified-fetch status to detect platform mode",
    }
]

# Soft contract: checked at --runtime as WARN, not blocking
SOFT_REQUIRED = [
    {
        "stage": "stage-3",
        "tool_group": ["mcp__unified-fetch__search", "mcp__unified-fetch__scrape", "mcp__unified-fetch__smart_browse"],
        "min_count": 1,
        "why": "Stage 3 verification requires actual search/browse/scrape unless C1 skip applies",
    },
    {
        "stage": "stage-0/1/2/5",
        "tool": "mcp__sequential-thinking__sequentialthinking",
        "min_count": 0,  # 0 = advisory; actual requirement depends on can_branch
        "why": "DAG branching when sequential-thinking available",
    },
]


def main():
    try:
        payload = json.load(sys.stdin)
    except Exception:
        sys.exit(0)  # malformed input: never fail the hook

    prompt = payload.get("prompt", "") or ""
    session_id = payload.get("session_id", "") or "unknown"

    # Only engage when this is a claude-reasoning invocation
    if "claude-reasoning" not in prompt:
        sys.exit(0)

    os.makedirs(CONTRACTS_DIR, exist_ok=True)

    # Extract mode/domain hints from prompt for the contract record
    mode_hint = None
    for m in ("diagnostic", "design", "decision", "optimization", "innovation", "general"):
        if re.search(r"\b" + m + r"\b", prompt):
            mode_hint = m
            break

    contract = {
        "session_id": session_id,
        "created_ts": int(time.time()),
        "mode_hint": mode_hint,
        "hard_required": HARD_REQUIRED,
        "soft_required": SOFT_REQUIRED,
        "note": "Generated by UserPromptSubmit hook. Model must not edit.",
    }

    path = os.path.join(CONTRACTS_DIR, session_id + ".json")
    with open(path, "w", encoding="utf-8") as f:
        json.dump(contract, f, ensure_ascii=False, indent=2)

    # Always allow the prompt
    print(json.dumps({"decision": "allow"}))


if __name__ == "__main__":
    main()
```

**Step 2: 測試生成器（直接餵 JSON）**

Run:
```bash
cd ~/.claude/skills/claude-reasoning
echo '{"prompt":"/claude-reasoning btc 2026 bottom","session_id":"test-session-1"}' | python3 scripts/contract-gen.py
cat ~/.claude/reasoning-contracts/test-session-1.json
```
Expected: 輸出 `{"decision": "allow"}`，contract 檔案包含 hard_required / soft_required

**Step 3: 測試非推理 prompt 不生成 contract**

Run:
```bash
echo '{"prompt":"修改 README.md","session_id":"test-session-2"}' | python3 scripts/contract-gen.py
ls ~/.claude/reasoning-contracts/ | grep test-session-2 && echo "FAIL: should not exist" || echo "PASS: not created"
```
Expected: `PASS: not created`

**Step 4: Commit**

```bash
git add scripts/contract-gen.py && git commit -m "feat: contract-gen.py for UserPromptSubmit hook"
```

---

## 任務 3: 寫 trail-log.sh（PostToolUse hook 記錄器）

**Files:**
- Create: `scripts/trail-log.sh`

**Step 1: 寫完整腳本**

```bash
#!/usr/bin/env bash
# Trail Logger — PostToolUse hook.
# Environment-append log of tool calls for reasoning sessions.
# The model never writes this file directly.
set -u

read -r stdin_payload || exit 0

# Only record when a contract exists for this session (kill switch)
session_id=$(printf '%s' "$stdin_payload" | python3 -c "import sys,json;print(json.load(sys.stdin).get('session_id',''))" 2>/dev/null || echo "")
[ -z "$session_id" ] && print_allow && exit 0

CONTRACTS_DIR="$HOME/.claude/reasoning-contracts"
TRAIL_DIR="$HOME/.claude/reasoning-trail"
CONTRACT="$CONTRACTS_DIR/$session_id.json"

# Kill switch: no contract => not a reasoning session => do nothing
[ -f "$CONTRACT" ] || { print_allow; exit 0; }

mkdir -p "$TRAIL_DIR"
TRAIL="$TRAIL_DIR/$session_id.jsonl"

tool_name=$(printf '%s' "$stdin_payload" | python3 -c "import sys,json;print(json.load(sys.stdin).get('tool_name',''))" 2>/dev/null || echo "")
tool_input=$(printf '%s' "$stdin_payload" | python3 -c "
import sys, json
try:
    d = json.load(sys.stdin)
    ti = d.get('tool_input', {}) or {}
    # keep only small scalar fields (query/url), drop bulky nested data
    slim = {}
    for k in ('query','url','focus','prompt'):
        if k in ti and isinstance(ti[k], str) and len(ti[k]) < 200:
            slim[k] = ti[k]
    print(json.dumps(slim, ensure_ascii=False))
except Exception:
    print('{}')
" 2>/dev/null || echo "{}")

ts=$(date +%s)

# Append one JSONL line (environment-generated, append-only)
printf '{"ts":%s,"tool":"%s","input":%s}\n' "$ts" "$tool_name" "$tool_input" >> "$TRAIL"

print_allow() {
  printf '{"decision":"allow"}'
}
print_allow
```

**Step 2: 測試（有 contract 時記錄）**

Run:
```bash
cd ~/.claude/skills/claude-reasoning
# ensure contract exists
echo '{"prompt":"/claude-reasoning test","session_id":"test-session-1"}' | python3 scripts/contract-gen.py >/dev/null
# simulate PostToolUse
echo '{"session_id":"test-session-1","tool_name":"mcp__unified-fetch__search","tool_input":{"query":"btc price"}}' | bash scripts/trail-log.sh
cat ~/.claude/reasoning-trail/test-session-1.jsonl
```
Expected: 輸出 `{"decision":"allow"}`，trail 檔案有一行記錄 tool=mcp__unified-fetch__search

**Step 3: 測試無 contract 時不記錄（kill switch）**

Run:
```bash
echo '{"session_id":"test-session-2","tool_name":"Bash","tool_input":{"command":"ls"}}' | bash scripts/trail-log.sh
ls ~/.claude/reasoning-trail/test-session-2.jsonl 2>/dev/null && echo "FAIL" || echo "PASS: not recorded"
```
Expected: `PASS: not recorded`

**Step 4: Commit**

```bash
git add scripts/trail-log.sh && git commit -m "feat: trail-log.sh for PostToolUse hook"
```

---

## 任務 4: 寫 gate-check.sh + gate-check.py（Stop hook 閘門）

**Files:**
- Create: `scripts/gate-check.py`
- Create: `scripts/gate-check.sh`

**Step 1: 寫 gate-check.py（核心判定）**

```python
#!/usr/bin/env python3
"""Gate Check — Stop hook core logic.

Compares trail vs contract. Prints JSON decision to stdout:
  {"decision": "continue"}                 -> allow stopping
  {"decision": "block", "reason": "..."}   -> prevent stop, inject missing list

Only hard_required items are blocking (no false positives on pure reasoning).
"""
import json
import os
import sys

CONTRACTS_DIR = os.path.expanduser("~/.claude/reasoning-contracts")
TRAIL_DIR = os.path.expanduser("~/.claude/reasoning-trail")


def main():
    try:
        payload = json.load(sys.stdin)
    except Exception:
        print(json.dumps({"decision": "continue"}))
        return

    session_id = payload.get("session_id", "") or ""
    contract_path = os.path.join(CONTRACTS_DIR, session_id + ".json")
    trail_path = os.path.join(TRAIL_DIR, session_id + ".jsonl")

    # Kill switch: no contract -> not a reasoning session -> allow
    if not os.path.exists(contract_path):
        print(json.dumps({"decision": "continue"}))
        return

    with open(contract_path, encoding="utf-8") as f:
        contract = json.load(f)

    # Load trail tool occurrences
    tool_counts = {}
    group_counts = []
    if os.path.exists(trail_path):
        with open(trail_path, encoding="utf-8") as f:
            for line in f:
                line = line.strip()
                if not line:
                    continue
                try:
                    entry = json.loads(line)
                    tool = entry.get("tool", "")
                    tool_counts[tool] = tool_counts.get(tool, 0) + 1
                except Exception:
                    continue

    missing = []
    for req in contract.get("hard_required", []):
        tool = req.get("tool")
        minc = req.get("min_count", 1)
        got = tool_counts.get(tool, 0)
        if got < minc:
            missing.append(f"- {req.get('stage')}: 需要調用 `{tool}` 至少 {minc} 次，trail 記錄為 {got} 次（{req.get('why','')}）")

    if missing:
        reason = (
            "🚧 **Execution Gate 攔截：推理流程未完成契約要求**\n\n"
            "以下強制工具調用缺失，無法停止。請補做後再輸出結論：\n\n"
            + "\n".join(missing)
            + "\n\n（此判定由 Stop hook 依 trail 生成，模型不可更改。）"
        )
        print(json.dumps({"decision": "block", "reason": reason}, ensure_ascii=False))
        return

    print(json.dumps({"decision": "continue"}))


if __name__ == "__main__":
    main()
```

**Step 2: 寫 gate-check.sh（bash 包裝）**

```bash
#!/usr/bin/env bash
# Gate Check — Stop hook wrapper. Delegates to gate-check.py.
set -u
read -r stdin_payload || exit 0
printf '%s' "$stdin_payload" | python3 "$(dirname "$0")/gate-check.py"
```

**Step 3: 測試 block（缺 A2 status）**

Run:
```bash
cd ~/.claude/skills/claude-reasoning
# fresh session with contract but empty trail
echo '{"prompt":"/claude-reasoning test","session_id":"gate-test-1"}' | python3 scripts/contract-gen.py >/dev/null
echo '{"session_id":"gate-test-1"}' | bash scripts/gate-check.sh
```
Expected: `{"decision":"block","reason":"🚧 ... mcp__unified-fetch__status ..."}`

**Step 4: 測試放行（有 A2 status trail）**

Run:
```bash
echo '{"session_id":"gate-test-1","tool_name":"mcp__unified-fetch__status","tool_input":{}}' | bash scripts/trail-log.sh >/dev/null
echo '{"session_id":"gate-test-1"}' | bash scripts/gate-check.sh
```
Expected: `{"decision":"continue"}`

**Step 5: 測試無 contract 放行（kill switch）**

Run:
```bash
echo '{"session_id":"nonexistent-session"}' | bash scripts/gate-check.sh
```
Expected: `{"decision":"continue"}`

**Step 6: Commit**

```bash
chmod +x scripts/gate-check.sh scripts/gate-check.py scripts/trail-log.sh scripts/contract-gen.py
git add scripts/gate-check.py scripts/gate-check.sh && git commit -m "feat: gate-check Stop hook with trail comparison"
```

---

## 任務 5: sync-check.sh 增加 --runtime 模式

**Files:**
- Modify: `scripts/sync-check.sh`

**Step 1: 在檔案開頭參數解析區加入 --runtime**

在 `set -u` 後加入：
```bash
RUNTIME_MODE=false
if [ "${1:-}" = "--runtime" ]; then
  RUNTIME_MODE=true
  shift
fi
```

**Step 2: 在檔案結尾（Step 9 之後、結束前）加入 runtime 檢查**

```bash
if [ "$RUNTIME_MODE" = true ]; then
  echo "== Step 10: Runtime trail audit (execution enforcement) =="
  CONTRACTS_DIR="$HOME/.claude/reasoning-contracts"
  TRAIL_DIR="$HOME/.claude/reasoning-trail"
  if [ ! -d "$CONTRACTS_DIR" ] || [ -z "$(ls -A "$CONTRACTS_DIR" 2>/dev/null)" ]; then
    echo "  [WARN] No reasoning contracts found (no /claude-reasoning sessions run)"
  else
    for contract_file in "$CONTRACTS_DIR"/*.json; do
      session_id=$(basename "$contract_file" .json)
      trail_file="$TRAIL_DIR/$session_id.jsonl"
      echo "  -- Session: $session_id"
      if [ ! -f "$trail_file" ]; then
        echo "    [FAIL] trail missing (stopped before any tool call)"
        continue
      fi
      python3 - "$contract_file" "$trail_file" <<'PYEOF'
import json, sys
contract_path, trail_path = sys.argv[1], sys.argv[2]
with open(contract_path, encoding="utf-8") as f:
    contract = json.load(f)
counts = {}
with open(trail_path, encoding="utf-8") as f:
    for line in f:
        line = line.strip()
        if not line: continue
        try:
            t = json.loads(line).get("tool", "")
            counts[t] = counts.get(t, 0) + 1
        except Exception: pass
for req in contract.get("hard_required", []):
    tool = req.get("tool"); got = counts.get(tool, 0)
    status = "[OK] " if got >= req.get("min_count", 1) else "[FAIL] "
    print(f"    {status}hard: {tool} x{got} (need {req.get('min_count',1)})")
for req in contract.get("soft_required", []):
    if "tool_group" in req:
        g = req["tool_group"]; got = sum(counts.get(t, 0) for t in g)
        status = "[OK] " if got >= req.get("min_count", 1) else "[WARN] "
        print(f"    {status}soft: {g[0]}... x{got} (need {req.get('min_count',1)})")
    else:
        tool = req.get("tool"); got = counts.get(tool, 0)
        print(f"    [INFO] soft: {tool} x{got} (advisory)")
PYEOF
    done
  fi
fi
```

**Step 3: 驗證 --runtime 模式**

Run:
```bash
cd ~/.claude/skills/claude-reasoning && bash scripts/sync-check.sh --runtime
```
Expected: 顯示 gate-test-1 session 的 trail 對比（status x1 OK）

**Step 4: Commit**

```bash
git add scripts/sync-check.sh && git commit -m "feat: sync-check --runtime trail audit mode"
```

---

## 任務 6: 配置全域 hooks（settings.json）

**Files:**
- Modify: `~/.claude/settings.json`

**Step 1: 讀取現有 settings.json**

Run:
```bash
cat ~/.claude/settings.json
```

**Step 2: 用 python 合併 hooks 配置（保留現有欄位）**

```bash
python3 - <<'PYEOF'
import json, os
path = os.path.expanduser("~/.claude/settings.json")
with open(path, encoding="utf-8") as f:
    s = json.load(f)
s["hooks"] = {
    "UserPromptSubmit": [
        {
            "matcher": "claude-reasoning",
            "hooks": [
                {"type": "command", "command": "bash ~/.claude/skills/claude-reasoning/scripts/contract-gen.sh"}
            ]
        }
    ],
    "PostToolUse": [
        {
            "hooks": [
                {"type": "command", "command": "bash ~/.claude/skills/claude-reasoning/scripts/trail-log.sh"}
            ]
        }
    ],
    "Stop": [
        {
            "hooks": [
                {"type": "command", "command": "bash ~/.claude/skills/claude-reasoning/scripts/gate-check.sh"}
            ]
        }
    ]
}
with open(path, "w", encoding="utf-8") as f:
    json.dump(s, f, ensure_ascii=False, indent=2)
print("hooks configured")
PYEOF
```

**注意**：contract-gen.sh 是包裝器，需要建立（簡化：把 contract-gen.py 的 stdin 讀取包一層，matcher 已在 hook config 層處理，腳本只需傳 stdin）：

**新增 `scripts/contract-gen.sh`：**
```bash
#!/usr/bin/env bash
# Contract Generator — UserPromptSubmit hook wrapper.
set -u
read -r stdin_payload || exit 0
printf '%s' "$stdin_payload" | python3 "$(dirname "$0")/contract-gen.py"
```

**Step 3: 驗證配置**

Run:
```bash
python3 -c "import json,os; s=json.load(open(os.path.expanduser('~/.claude/settings.json'))); print(json.dumps(s['hooks'], indent=2))"
```
Expected: 三個 hook（UserPromptSubmit/PostToolUse/Stop）正確顯示

**Step 4: Commit（此為全域檔，不屬 repo——只確認記錄在 C2 對齊清單）**

（settings.json 不在 claude-reasoning repo 內，無需 git commit；在本計劃的驗證閘門中標記 ✅）

---

## 任務 7: E2E 實測（驗證全部防線）

**Files:**
- Verify: 無

**Step 1: 清空測試軌跡 + 建立新 session contract**

Run:
```bash
rm -f ~/.claude/reasoning-trail/e2e-* ~/.claude/reasoning-contracts/e2e-*
echo '{"prompt":"/claude-reasoning diagnostic test bug","session_id":"e2e-1"}' | bash ~/.claude/skills/claude-reasoning/scripts/contract-gen.sh
```

**Step 2: 模擬「模型想跳步停止」——缺 A2 status**

Run:
```bash
echo '{"session_id":"e2e-1"}' | bash ~/.claude/skills/claude-reasoning/scripts/gate-check.sh
```
Expected: **block** + 缺失清單

**Step 3: 模擬「模型補做 A2」——加 trail 後再試**

Run:
```bash
echo '{"session_id":"e2e-1","tool_name":"mcp__unified-fetch__status","tool_input":{}}' | bash ~/.claude/skills/claude-reasoning/scripts/trail-log.sh >/dev/null
echo '{"session_id":"e2e-1"}' | bash ~/.claude/skills/claude-reasoning/scripts/gate-check.sh
```
Expected: **continue**（硬性契約通過）

**Step 4: 模擬「偽造記錄」——模型在日誌寫 search x6 但 trail 無**

用 sync-check --runtime 檢驗：模型日誌的 `search_count: 6` 無法被 trail（真實環境）支持 → 對比暴露

Run:
```bash
cd ~/.claude/skills/claude-reasoning && bash scripts/sync-check.sh --runtime
```
Expected: e2e-1 session 顯示 `hard: mcp__unified-fetch__status x1 [OK]`、`soft: search... x0 [WARN]`

**Step 5: 清理測試檔案**

```bash
rm -f ~/.claude/reasoning-trail/e2e-* ~/.claude/reasoning-contracts/e2e-* \
      ~/.claude/reasoning-trail/test-session-* ~/.claude/reasoning-contracts/test-session-* \
      ~/.claude/reasoning-trail/gate-test-* ~/.claude/reasoning-contracts/gate-test-*
```

---

## 任務 8: SKILL.md 對齊 + CHANGELOG + README

**Files:**
- Modify: `SKILL.md`
- Modify: `CHANGELOG.md`
- Modify: `README.md`

**Step 1: SKILL.md 加入 execution enforcement 章節**

在「MCP Toolchain」後加入：

```markdown
## Execution Enforcement (Hooks)

推理流程由環境強制執行，不依賴模型自律：

| Hook | 角色 | 機制 |
|------|------|------|
| `UserPromptSubmit` | 契約生成 | 偵測 `/claude-reasoning` → 生成 `~/.claude/reasoning-contracts/{session}.json`（必需工具序列）|
| `PostToolUse` | 軌跡記錄 | 每次工具調用 → 環境追加 `~/.claude/reasoning-trail/{session}.jsonl`（模型不參與）|
| `Stop` | 閘門 | 模型停止前對比 trail vs 契約 → 缺失則 block + 注入缺失清單 |

**硬性契約**：A2 執行規則 1 的 `mcp__unified-fetch__status` 必須實際調用（無例外）。
**軟性契約**：Stage 3 搜索/爬取、sequential-thinking 調用，由 `sync-check.sh --runtime` 以 WARN 審計。
**Kill switch**：無契約的 session 完全不受影響；對抗性篡改（模型用工具改 trail）為物理不可防，誠實聲明。
```

**Step 2: CHANGELOG 加入 v1.1.1（或確認版本策略）**

在 CHANGELOG.md 最上方加入：
```markdown
## v1.1.1 (2026-08-23)
**Execution Trail Enforcement — 環境強制推理流程**
1. **Contract 層** — `UserPromptSubmit` hook 生成 session 執行契約（硬/軟必需工具序列）
2. **Trail 層** — `PostToolUse` hook 環境追加工具調用軌跡，模型不可偽造
3. **Gate 層** — `Stop` hook 對比 trail vs 契約，缺失則 block，阻止當次跳步驟
4. **審計** — `sync-check.sh --runtime` 對比全域 session trail，暴露偽造執行記錄
5. **腳本** — `contract-gen.py/.sh`、`trail-log.sh`、`gate-check.py/.sh`
```

**Step 3: README 對齊（MCP/Hook 章節）**

- 執行模式表加入「Hook 強制」列
- 新增「Execution Enforcement」段落（內容與 SKILL.md 一致）

**Step 4: 驗證 + Commit**

```bash
cd ~/.claude/skills/claude-reasoning && bash scripts/sync-check.sh
git add SKILL.md CHANGELOG.md README.md && git commit -m "docs: execution enforcement alignment v1.1.1"
```

---

## 任務 9: 完整驗證閘門 + tag

**Step 1: 全量檢查**

Run:
```bash
cd ~/.claude/skills/claude-reasoning && bash scripts/sync-check.sh && bash scripts/sync-check.sh --runtime
```
Expected: `All checks passed` + runtime 審計無 FAIL

**Step 2: 版本落地**

```bash
git tag -a v1.1.1 -m "v1.1.1: execution trail enforcement"
git push origin master --tags
```

**Step 3: 對齊閘門確認**

| 對齊項 | 狀態 |
|--------|------|
| SKILL.md version | v1.1.1 |
| CHANGELOG ## v1.1.1 | ✅ |
| Git tag v1.1.1 | ✅ |
| GitHub Release | 視需要 `gh release create v1.1.1` |
| `sync-check --runtime` 無 FAIL | ✅ |
| settings.json hooks 已配置 | ✅ |